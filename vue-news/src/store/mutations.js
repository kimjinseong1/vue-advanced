export default {
        set_news(state, news) {
            state.news = news;
        },
        set_jobs(state, jobs) {
            state.jobs=jobs;
        },
        set_ask(state, ask) {
            state.ask=ask;
        },
        set_user(state, user) {
            state.user=user;
        },

}